import { styled } from '../../styles'

export const TooltipContainer = styled('div', {
  background: '$gray900',
  padding: '$3 $4',
  borderRadius: '$sm',
  color: '$white',
  fontFamily: '$default',
  fontSize: '$sm',
})
